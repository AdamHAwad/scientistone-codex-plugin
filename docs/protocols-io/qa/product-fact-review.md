# Independent product-fact review

Review date: August 27, 2026

Scope: Current Codex permissions, app updates, marketplace refresh behavior, and the Scientist1 interface.

## Result

Pass, with one time-sensitive note.

- The current permission label is **Approve for me**. It uses a separate AI reviewer for eligible requests and does not remove the sandbox or every user decision.
- The desktop app normally updates itself. A full quit and reopen is a clear beginner action when no study is running.
- Codex checks Git marketplaces when it starts. OpenAI also documents **Plugins** > **Refresh** as the manual way to fetch a newer marketplace version.
- The guide does not promise that every plugin update appears during an already-open task. It tells the reader to start a new task after a refresh.
- The Scientist1 setup labels and state labels in the guide match the captured interface.

Product labels can change. Recheck these instructions before publishing a new protocol version.
